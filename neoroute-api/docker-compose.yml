version: "3.9"

services:
  db:
    image: postgres:15
    container_name: postgres-local
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: neoroute
    ports:
      - "5432:5432"